version: "3"

services:
    backend:
        command: python manage.py runserver 0.0.0.0:80
        volumes:
            - ./backend:/code
        ports:
            - "8000:80"
    frontend:
        command: npm run serve
        volumes:
            - ./frontend:/app
        ports:
            - "80:8080"

